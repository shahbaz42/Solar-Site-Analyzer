# Solar Site Analyzer - Docker Compose Configuration

# Database Configuration
DB_USER=solaruser
DB_PASSWORD=solarpassword
DB_NAME=solar_site_analyzer

# Redis Configuration
REDIS_ENABLED=True
REDIS_TTL=300

# API Configuration
DEBUG=False

# Frontend Configuration
# Note: These values are embedded at BUILD TIME for Docker
# After changing these values, rebuild the frontend: docker compose build frontend
VITE_API_BASE_URL=http://localhost:8000
VITE_MAPBOX_TOKEN=your_mapbox_access_token_here
